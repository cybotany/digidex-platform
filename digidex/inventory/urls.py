from rest_framework.routers import DefaultRouter
from django.urls import include, path

#from nfc import views, api

# router = DefaultRouter()
# router.register(r'tags', api.NearFieldCommunicationTagViewSet)
#router.register(r'links', api.NearFieldCommunicationLinkViewSet)

# app_name = 'nfc'
# urlpatterns = [
#     path('api/', include(router.urls)),
#     path('<uuid:link_uuid>/', views.route_nfc_link, name='route_nfc'),
#     path('<uuid:link_uuid>/mapping/', views.map_nfc_link, name='map_nfc'),
# ]
